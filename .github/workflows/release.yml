name: Release

on:
  push:
    tags:
      - "v*"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build-release-artifacts:
    name: Build Release Artifact (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-latest
            target_triple: x86_64-unknown-linux-gnu
          - os: macos-latest
            target_triple: aarch64-apple-darwin
          - os: windows-latest
            target_triple: x86_64-pc-windows-msvc

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Resolve Version
        id: version
        shell: bash
        run: |
          if [[ "${GITHUB_REF_TYPE}" == "tag" ]]; then
            echo "value=${GITHUB_REF_NAME#v}" >> "$GITHUB_OUTPUT"
          else
            echo "value=0.1.0-dev" >> "$GITHUB_OUTPUT"
          fi

      - name: Build Package
        shell: bash
        env:
          CLAWGUARD_RELEASE_KEY_ID: github-release
          CLAWGUARD_RELEASE_PRIVATE_KEY_FILE: ${{ runner.temp }}/release-private.pem
          CLAWGUARD_REQUIRE_SIGNATURE: "1"
        run: |
          cat > "${CLAWGUARD_RELEASE_PRIVATE_KEY_FILE}" <<'EOF'
          ${{ secrets.RELEASE_SIGNING_PRIVATE_KEY }}
          EOF
          VERSION="${{ steps.version.outputs.value }}" \
          TARGET_TRIPLE="${{ matrix.target_triple }}" \
          bash scripts/package-release.sh

      - name: Upload Artifact
        uses: actions/upload-artifact@v4
        with:
          name: clawguard-${{ steps.version.outputs.value }}-${{ matrix.target_triple }}
          path: |
            artifacts/*.tar.gz
            artifacts/*.sig

  publish-github-release:
    name: Publish GitHub Release
    runs-on: ubuntu-latest
    needs: build-release-artifacts
    if: github.ref_type == 'tag'

    steps:
      - name: Download Release Artifacts
        uses: actions/download-artifact@v4
        with:
          path: dist
          pattern: clawguard-*
          merge-multiple: true

      - name: Publish Release
        uses: softprops/action-gh-release@v2
        with:
          files: dist/*.tar.gz
